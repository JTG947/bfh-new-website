/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    i18n: {
        locales : ['en', 'fr', ], // Add all supported languages
        defaultLocale: 'en', // Set to the defult local language
    },
    //other nextjs configurations
};
